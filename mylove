<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Do You Love Me?</title>
<style>
  body {
    background: linear-gradient(to top right, #ffdde1, #ee9ca7);
    font-family: "Comic Sans MS", cursive, sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    height: 100vh;
    margin: 0;
    overflow: hidden;
  }

  .bears {
    position: relative;
    width: 300px;
    height: 200px;
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
  }

  .bear {
    width: 120px;
    height: 150px;
    background: #6b4e3d;
    border-radius: 60px 60px 40px 40px;
    position: relative;
    transition: all 1s ease;
  }

  .bear::before, .bear::after {
    content: "";
    position: absolute;
    background: #6b4e3d;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    top: -20px;
  }

  .bear::before {
    left: 10px;
  }

  .bear::after {
    right: 10px;
  }

  .ear-inner {
    position: absolute;
    width: 20px;
    height: 20px;
    background: #b58264;
    border-radius: 50%;
    top: -10px;
  }

  .ear-inner.left {
    left: 20px;
  }

  .ear-inner.right {
    right: 20px;
  }

  .face {
    position: absolute;
    width: 100%;
    height: 70px;
    top: 30px;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .eye {
    width: 15px;
    height: 15px;
    background: #000;
    border-radius: 50%;
    margin: 0 15px;
  }

  .nose {
    position: absolute;
    top: 50px;
    width: 20px;
    height: 15px;
    background: #000;
    border-radius: 50%;
    left: 50%;
    transform: translateX(-50%);
  }

  .text {
    margin-top: 40px;
    font-size: 2em;
    color: #4b2e2b;
    text-shadow: 1px 1px 2px #fff;
  }

  .buttons {
    margin-top: 20px;
  }

  button {
    padding: 10px 25px;
    font-size: 1.2em;
    border: none;
    border-radius: 12px;
    margin: 10px;
    cursor: pointer;
    transition: 0.2s;
  }

  #yes {
    background-color: #ff8fab;
    color: white;
  }

  #no {
    background-color: #b6b6b6;
    color: white;
    position: relative;
  }

  #yes:hover {
    background-color: #ff4f75;
  }

  .hug {
    justify-content: center;
  }

  .hearts {
    position: absolute;
    top: -40px;
    left: 50%;
    transform: translateX(-50%);
    font-size: 2em;
    color: red;
    animation: floatHearts 2s ease-in-out infinite;
  }

  @keyframes floatHearts {
    0% { transform: translateX(-50%) translateY(0); opacity: 1; }
    100% { transform: translateX(-50%) translateY(-40px); opacity: 0; }
  }

  @keyframes playAround {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
  }

  .bear {
    animation: playAround 1.5s infinite;
  }
</style>
</head>
<body>
  <div class="bears">
    <div class="bear" id="boy">
      <div class="ear-inner left"></div>
      <div class="ear-inner right"></div>
      <div class="face">
        <div class="eye"></div>
        <div class="eye"></div>
      </div>
      <div class="nose"></div>
    </div>

    <div class="bear" id="girl" style="background:#8b5e3c;">
      <div class="ear-inner left"></div>
      <div class="ear-inner right"></div>
      <div class="face">
        <div class="eye"></div>
        <div class="eye"></div>
      </div>
      <div class="nose"></div>
    </div>
  </div>

  <div class="text">Do you love me?</div>

  <div class="buttons">
    <button id="yes">Yes 💖</button>
    <button id="no">No 💔</button>
  </div>

<script>
  const noBtn = document.getElementById("no");
  const yesBtn = document.getElementById("yes");
  const bears = document.querySelector(".bears");
  const text = document.querySelector(".text");

  noBtn.addEventListener("mouseenter", () => {
    const x = Math.random() * window.innerWidth * 0.6;
    const y = Math.random() * window.innerHeight * 0.6;
    noBtn.style.position = "absolute";
    noBtn.style.left = `${x}px`;
    noBtn.style.top = `${y}px`;
  });

  yesBtn.addEventListener("click", () => {
    text.textContent = "Awww! They hug ❤️";
    bears.classList.add("hug");
    bears.innerHTML = `
      <div class="bear" id="boy" style="transform: translateX(30px);">
        <div class="ear-inner left"></div>
        <div class="ear-inner right"></div>
        <div class="face">
          <div class="eye"></div>
          <div class="eye"></div>
        </div>
        <div class="nose"></div>
        <div class="hearts">❤️❤️❤️</div>
      </div>
      <div class="bear" id="girl" style="transform: translateX(-30px); background:#8b5e3c;">
        <div class="ear-inner left"></div>
        <div class="ear-inner right"></div>
        <div class="face">
          <div class="eye"></div>
          <div class="eye"></div>
        </div>
        <div class="nose"></div>
      </div>
    `;
    document.querySelector(".buttons").remove();
  });
</script>
</body>
</html>
